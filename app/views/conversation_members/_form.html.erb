<%= form_for(conversation_member) do |f| %>
  <% if conversation_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation_member.errors.count, "error") %> prohibited this conversation_member from being saved:</h2>

      <ul>
      <% conversation_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :conversation_id %>
    <%= f.text_field :conversation_id %>
  </div>

  <div class="field">
    <%= f.label :profile_id %>
    <%= f.text_field :profile_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
